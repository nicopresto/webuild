{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "authoring-theme",
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
  "run:site": "cd packages/blog && yarn develop",
  "run:theme": "cd packages/shop && yarn develop",
  "run:all": "npm-run-all --parallel run:site run:theme"
  }
}
